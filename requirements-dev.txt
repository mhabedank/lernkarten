# For working on lernkarten. End users need none of this: the runtime fetches
# what it needs itself, via scripts/deps.py.
# Tools are pinned exactly, so formatting never shifts under a contributor;
# libraries get a range instead.
pytest>=9.1.1
ruff==0.16.5
# Writes the one JPEG in the test fixture (scripts/make_testdata.py). Replaces a
# sips/magick/convert lottery that only ever paid out on macOS.
pillow>=11,<13
# The runtime dependency, listed here too so a checkout can run the tests without
# waiting for scripts/deps.py to fetch it. scripts/deps.py holds the real pin.
pyyaml==6.0.3
# The optional runtime dependency (scripts/deps.py FIGURES): pulls figures off
# PDF pages for /ingest. Listed here for the same reason as pyyaml — and because
# a test that renders a real PDF cannot monkeypatch its way to one.
pypdfium2==5.13.0
